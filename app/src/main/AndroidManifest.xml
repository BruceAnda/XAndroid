<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.zhaoliang5156.xandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.basic.BasicActivity" />
        <activity android:name=".ui.activity.basic.quickstart.QuickStartActivity" />
        <activity android:name=".ui.activity.basic.quickstart.demos.helloworld.HelloWorldActivity" />
        <activity android:name=".ui.activity.basic.quickstart.demos.simpledownloadui.SimpleDownloadUIActivity" />
        <activity android:name=".ui.activity.basic.quickstart.demos.simplelight.SimpleLightActivity"></activity>
    </application>

</manifest>